from distutils.core import setup
setup(
  name = 'boolazy',
  packages = ['boolazy'], # this must be the same as the name above
  version = '0.1',
  description = 'Too lazy for proper bool syntax? Don\'t worry, be lazy.',
  author = 'Josef Lange',
  author_email = 'josef.d.lange@me.com',
  url = 'https://github.com/josefdlange/boolazy', # use the URL to the github repo
  download_url = 'https://github.com/josefdlange/boolazy/tarball/0.1', # I'll explain this in a second
  keywords = ['boolean', 'lazy'], # arbitrary keywords
  classifiers = [],
)